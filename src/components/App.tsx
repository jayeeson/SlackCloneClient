import React from 'react';
import AppContainer from './AppContainer';
import Layout from './Layout';

const App = () => {
  return (
    <React.Fragment>
      <AppContainer>
        <Layout />
      </AppContainer>
    </React.Fragment>
  );
};

export default App;
